#include "main.h"
/**
 * _abs - checks for absolute values of a number
 * @n: holds n
 * Return: positive number
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
